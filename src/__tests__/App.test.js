import { render, screen } from '@testing-library/react';
import TestRenderer from 'react-test-renderer';
import App from '../App';

test('renders learn react link', () => {
  //render(<App />);
  /* const testRenderer = TestRenderer.create(<App />);
  const testInstance = testRenderer.root;
  expect(testInstance.findByProps({className: "App-header"}).children[2].props.href).toEqual("https://reactjs.org"); */


  //console.log(testInstance.findByProps({className: "App-header"}).children[2].props.href);
  //const linkElement = screen.getByText(/REACT/i);
  //expect(linkElement).toBeInTheDocument();
});

test('Show all pokemons', () =>{

});

test('Search a specific pokemon', () =>{
  
});

test('Add a new pokemon', () =>{
  
});

test('Edit a specific pokemon', () =>{
  
});

test('Delete a specific pokemon', () =>{
  
});
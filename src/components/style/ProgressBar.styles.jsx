import styled from '@emotion/styled';

const Wrapper = styled.div`
  .styled-progressbar{
    display: flex;
    align-items: center;
  }
  .limit-side{
    font-size: 24px;
  }
  .text{
    padding-right: 15px;
    width: 130px;
  }
`;
export default Wrapper;